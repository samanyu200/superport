# Superport

Simple CLI port forwarding tool using SSH reverse tunnels.

## Install (One Command)
```bash
apt update && apt install -y git && git clone https://github.com/samanyu200/superport && chmod +x superport/install.sh && bash superport/install.sh
